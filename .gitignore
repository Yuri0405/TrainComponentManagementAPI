#------------------------------------------------------------------------------
# GENERAL
#------------------------------------------------------------------------------
# Ignore OS generated files
.DS_Store
Thumbs.db
ehthumbs.db
ehthumbs_windows.db

# Ignore Rider and ReSharper specific files and directories
.idea/
*.sln.ide/
*.[Rr]e[Ss]harper
*_ReSharper?*/

# Ignore Visual Studio specific files and directories
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.*proj.user
*.*csproj.user

# Ignore other IDE/Editor specific files
*.swp # Vim swap files

#------------------------------------------------------------------------------
# .NET CORE / .NET 5+
#------------------------------------------------------------------------------
# Ignore build output folders
**/bin/
**/obj/

# Ignore NuGet packages folder (less common with PackageReference, but good practice)
**/packages/
# Keep package dependency files
!**/packages.config
!**/project.json
# Ignore generated NuGet files in source directories
*.nuget.props
*.nuget.targets

# Ignore user secrets files (contains sensitive data!)
# Use 'dotnet user-secrets set <key> <value>' for managing secrets.
secrets.json
**/secrets.json

# Ignore local environment configuration files (often contain secrets)
.env

# Ignore publish profiles that might contain user-specific sensitive data
# The main .pubxml file itself can often be safely committed if it doesn't contain passwords.
**/Properties/PublishProfiles/*.pubxml.user
Properties/PublishProfiles/*.pubxml.user

# Ignore files produced by dotnet-watch
**/node_modules/*/.bin/

# Ignore log files and temporary files
*.log
logs/
*.log.*
*.tmp
*.bak
*.pidb

# Ignore test results and code coverage files
TestResults/
**/TestResults/
*.trx
*.coverage
*.coveragexml

# SQL Server Data Tools (SSDT) files
*.dbmdl
*.jfm

# Entity Framework Core Migrations cache (if exists)
# IMPORTANT: Usually the actual Migration files (*.cs) in the Migrations folder ARE committed.
# Microsoft.EntityFrameworkCore.Design.dll

#------------------------------------------------------------------------------
# WEB SPECIFIC (ASP.NET Core, Node.js integration)
#------------------------------------------------------------------------------
# Ignore node_modules directory (if using Node.js for client-side tooling/builds)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore client-side build output folders (adjust if your output path is different)
**/wwwroot/dist/
**/wwwroot/lib/ # If using tools like libman or bower that download to wwwroot/lib

# Ignore bundler/minifier cache/config (if using specific tools)
# bundleconfig.json.bindings
#দারুণ!দারুণ!দারুণ!দারুণ!দারুণ!দারুণ! [global]
# webpack.config.js (if environment specific and contains secrets)

#------------------------------------------------------------------------------
# OTHER COMMON
#------------------------------------------------------------------------------
# Ignore files generated by debugging tools (like profilers)
*.psess
*.vsp
*.vspx
*_i.exe
*_p.exe

# Ignore generated documentation files
*_APIs.xml

# Ignore Rider's local history (though usually stored outside project)
# Rider files outside .idea/ are typically global config, not per-project